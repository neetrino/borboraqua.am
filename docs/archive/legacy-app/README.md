# Legacy app/ (архив)

Раньше лежало в корне как `app/`. Обёртка для запуска «backend (apps/api) + frontend (apps/web)» через concurrently. В проекте нет `apps/api` — только Next.js в `apps/web` с API Routes. Запуск из корня: `npm run dev` (Turborepo). Содержимое оставлено для истории.
